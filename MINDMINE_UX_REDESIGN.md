# MindMine Core Experience Redesign — UX Proposal

> 状态：**待确认，本轮不修改任何代码**
> 基于：`PRD.md` / `TECH_DESIGN.md` / `AGENTS.md` / Phase 1 前端实现（7 个 Vue 文件，2548 行）
> 日期：2026-09-13

---

## 0. 诊断结论

当前实现在**功能上完整**，但在**叙事上是错的**。

问题的根源只有一句话：

> 现在的界面在**汇报系统状态**，而不是在**呈现用户的思想过程**。

具体表现为三个错位：

| 维度 | 现在是 | 应该是 |
|---|---|---|
| 界面主语 | AI 在说话、AI 在总结、AI 在提问 | 用户的话被排列、被连接、被命名 |
| 信息组织 | 并列（三张卡、六个字段、四段 chain） | 聚焦（一次一件事） |
| 状态变化 | 切换（v-if 换一个 section） | 转变（同一个空间发生形变） |

所以这一轮的动作不是"加设计"，是**删掉 40% 的 UI，把省下的空间全部给一件事：让用户看见自己的想法在成形**。

---

## 1. 当前每个页面为什么体验弱

### 1.1 LandingPage —— 说明书语气，承诺没有被演示

**现状**：Slogan → 三行产品介绍 → 演示数据免责声明 → 三组选择题（身份 / 领域 / 分享偏好）→ 按钮。

**问题**：
- `intro` 三行文案试图把整个产品讲清楚（"不替你写答案""结构化对话""知乎观点挑战"）。但用户在第一屏根本无法理解"挖掘"是什么，这些话是**空承诺**。
- 画像表单有 5+8+4 = 17 个可点项，**填表负担出现在用户产生任何兴趣之前**。这是典型的"先要数据，再给价值"。
- `phase-note`（演示数据免责）在第一屏就出现，等于开场先说"这是假的"。合规必要，但位置错了。

### 1.2 QuestionsPage —— 最严重的定位偏差：它像一个推荐列表

**现状**：标题 + 副标题 + 免责 + 三张平铺卡片，每张有 tag、演示角标、标题、"为什么推荐给你"色块、按钮。

**问题**：
- **三张卡片同时出现 = 三个选择题**。用户进入的是"挑一个"的比价心态，而不是"这个说的是我"的认领心态。
- 每张卡信息密度过高：2 个 tag + 1 个标题 + 1 个带边框的 why 色块 + 1 个全宽按钮。**5 个视觉元素 × 3 张 = 15 个元素**竞争注意力。
- `why_fits` 被包在 `.why` 色块里、加了 `为什么推荐给你` 标签——这个措辞是**推荐系统语气**，不是**认领语气**。"推荐给你"意味着系统在挑选；"你可能有资格回答"意味着用户被看见。
- 按钮文案"我有话说"本身是对的，但被渲染成 `.btn-primary` 全宽方块，和"提交表单"没有视觉区别，情绪被抹平了。

### 1.3 MinePage / 访谈阶段 —— 右侧面板是开发者视角的泄漏

**现状**：左侧固定高度 chat（`height: calc(100vh - 170px)`），右侧 340px `KnowledgePanel`，展示 6 个分组：已发现经历 / 关键事件 / 当时的信念 / 冲突 / 反思 / 潜在 Insight，带总数计数器。

**问题**：
- **`KnowledgeState` 是内部数据结构，不该原样上屏**。"beliefs""conflicts""candidate_insights"是给工程师和 Prompt 看的，普通用户看到的是一个**后台管理系统**。
- 计数器 `{{ total }}` 把体验变成刷分。用户会去想"我怎么让这个数字变大"，而不是"我在回忆什么"。
- 6 个分组同时可见时，Demo 结尾会有 10 条文字堆在 340px 宽的栏里，**每条都没有分量**。而 Insight 的力量恰恰来自"只有少数几件事，但它们互相勾连"。
- 面板文案"这些全部来自你说过的话"是对的，但它在**用说明弥补设计**——如果视觉本身没让人感到是自己的话，加一行字没用。
- 左侧是标准 chat 气泡（头像 + 圆角泡 + 打字点点）。**这是 ChatGPT 的默认长相**，直接触发"我在用一个聊天机器人"的心理模型。

### 1.4 MinePage / Insight 阶段 —— 最严重的问题：它是一次页面切换，不是一次揭示

**现状**：`phase === 'insight'` 时整块 `v-else-if` 换成一张居中卡片，结构是「✨ I found something.」→ 你最开始说 → ↓ → 但你真正想表达的可能是 → 三个按钮。

**问题**：
- **`v-if` / `v-else-if` 的硬切换在语义上就是错的**。用户的经历"消失"了，一张 AI 的卡片"出现"了。这精确地传达了错误的信息：Insight 是 AI 给的，不是从我的话里长出来的。
- 右侧 `KnowledgePanel` 在此刻**仍然原封不动地挂着**（`layout` 网格外层没变）。所有 Fragment 还在角落列着，中央却在放一张无关的卡——**最该被连接的素材，在最该发生连接的时刻，是静止的**。
- `✨ I found something.` 的主语是 **I（AI）**。这一句话就把整个产品的立场说反了。
- 「surface_claim → ↓ → deep_insight」的两段式，是**一次总结**的形状，不是**一次发现**的形状。总结可以一眼读完，发现必须有时间感。
- 暖金色 + `radial-gradient` + `box-shadow` 是**装饰性**的高光，不是**状态性**的高光。周围环境没有变暗，所以卡片不"亮"。

### 1.5 MinePage / Challenge 阶段 —— AI 又在提问，用户的观点不在场

**现状**：一张挑战卡（角标 + kicker + claim + reason + 来源行）→ 一个"它对你的提问"区块 → Demo 推荐输入 → 输入框。

**问题**：
- **用户刚刚确认的 Insight V1 完全从屏幕上消失了**。既然是"我的观点遇到另一个人的观点"，我的观点必须在场。现在屏幕上只有对方，那就还是"AI 在考我"。
- `challenge_question`（"它对你的提问"）是**AI 代替对方发问**，多了一层中介。真正的对峙不需要主持人。
- 上下排列 = 顺序阅读 = 没有冲突感。冲突在视觉上只能由**对称的空间对立**表达。
- 卡片信息层级混乱：`claim` 和 `reason` 用相近字号并列，读者不知道哪句是"他的观点"。

### 1.6 MinePage / Evolution 阶段 —— 四个盒子的流程图，不是思想的变化

**现状**：`evo-chain` 四节点垂直排列：Insight V1 ↓ Community Challenge ↓ Your Response ↓ Insight V2。

**问题**：
- 这是**系统流程的可视化**，不是**观点变化的可视化**。它在说"我们走完了 4 步"，而不是"你的判断哪里变了"。
- V1 和 V2 是**两段独立的文字**，用户必须自己逐字比对才能发现差异。产品最该证明的"你想深了一层"，被交给用户自己去找。
- 标题"你的判断刚刚变得更锋利了"是**AI 在替用户下结论**。这句话应该由用户在心里说出来，不是印在屏幕上。

### 1.7 ResultPage —— 结构没问题，但仪式感落在了错的地方

**现状**：`STAGE 6 — 完成` 标签 + 标题 + 副标题 + 核心 Insight 卡 + 回答正文 + 字数 + 操作按钮。

**问题**：
- `STAGE 6 — 完成` 这类阶段编号贯穿全站（Landing 无、Questions 是 `STAGE 2`、Result 是 `STAGE 6`），**暴露了内部状态机**，且编号不连续、含义不明。
- "下面每一句话都来自你说过的经历，没有一句是凭空生成的"——又是**用文案承诺体验**。如果前面的挖掘过程做对了，这句话不必说。
- 回答正文（655 字）是页面上最大的一块，但它不是 MindMine 的价值主张。**价值是 Insight，回答只是副产品**。当前排版让副产品占了主位。

---

## 2. 哪些信息应该删除

按"删除后体验更强"排序：

| # | 删除对象 | 位置 | 理由 |
|---|---|---|---|
| D1 | **`KnowledgePanel` 的 6 个字段分组与全部字段名** | `KnowledgePanel.vue` | 内部数据结构泄漏，开发者视角 |
| D2 | **Knowledge 条目总数计数器** | `KnowledgePanel.vue` | 把思考变成刷分 |
| D3 | **同屏三张问题卡** | `QuestionsPage.vue` | 比价心态取代认领心态 |
| D4 | **问题卡上的 `tag` / `演示` 双角标、`为什么推荐给你` 标签文字** | `QuestionsPage.vue` | 推荐系统语气 |
| D5 | **顶部 6 段式进度条**（发现/经历/反思/洞察/挑战/进化） | `MinePage.vue` `STATE_STEPS` | 暴露状态机；且让 Insight 变成"第 4 步"，剧透了惊喜 |
| D6 | **`STAGE 2` / `STAGE 6` 阶段编号标签** | `QuestionsPage` / `ResultPage` | 内部编号，对用户无意义 |
| D7 | **`✨ I found something.`** | `MinePage.vue` | 主语是 AI，立场错误 |
| D8 | **`它对你的提问` 区块及 `challenge_question`** | `MinePage.vue` | 多余中介层，削弱直接对峙 |
| D9 | **`evo-chain` 的 Community Challenge / Your Response 两个中间节点** | `MinePage.vue` | 流程可视化 ≠ 思想可视化 |
| D10 | **Landing 的三行 `intro` 产品说明** | `LandingPage.vue` | 在用户能理解之前的空承诺 |
| D11 | **Landing 首屏的演示数据免责块** | `LandingPage.vue` | 合规保留，但下沉到页脚 |
| D12 | **各页"用文案解释体验"的副标题**（"这些全部来自你说过的话""没有一句是凭空生成的""你的判断刚刚变得更锋利了"） | 多处 | 体验对了不用说，体验不对说了也没用 |
| D13 | **`btn-secondary disabled title="Phase 2 启用"` 的占位按钮** | `MinePage.vue` | 灰按钮是噪声，Phase 1 不做就不显示 |
| D14 | **骨架屏 shimmer 动画** | `QuestionsPage.vue` | 本地 Mock 响应 < 50ms，闪一下更糟 |

**保留但视觉降级**（不删，因为 Demo 需要）：
- `Demo 推荐输入` → 改为输入框下方一行极淡的可点文字，去掉橙色标签框。
- `is_mock` 标注 → 保留，但从"角标"改为对峙卡片底部一行 11px 灰字。**合规红线不动**（AGENTS.md P5）。

---

## 3. 哪些交互应该重构

| # | 交互 | 现在 | 改为 |
|---|---|---|---|
| R1 | 选问题 | 三卡平铺，点按钮 | 单问题全屏，点整块区域；「换一个」是次要动作 |
| R2 | 知识展示 | 6 组字段列表 | 5–7 个 **Thought Fragment**，无字段名 |
| R3 | 进入 Insight | `v-if` 切换 section | **同一空间内的三幕形变**，聊天区不消失只退后 |
| R4 | Fragment 归宿 | 一直待在右侧栏 | Insight 时**从右侧栏飞入画面中心并重排** |
| R5 | Challenge 呈现 | 上下卡片 | **左右对峙**，用户的 V1 在左侧在场 |
| R6 | Challenge 追问 | AI 转述的 `challenge_question` | 固定一句：**「他哪里说对了？又哪里说错了？」** |
| R7 | V1→V2 | 两段独立文字 | **同一段文字原地改写**（删除线淡出 + 限定词落入） |
| R8 | 阶段感知 | 顶部 6 点进度条 | 删除。改为问题标题常驻 + 极轻的 3 点（挖掘 / 碰撞 / 成形） |
| R9 | 页面滚动 | Insight/Challenge 在长页面里滚动 | 关键时刻**锁定视口**，一屏一件事 |

---

## 4. Question Discovery 新设计

### 目标句

> 让用户第一次意识到：**原来这些问题，我可能真的有资格回答。**

### 结构变化

**（a）画像下沉、轻量化**

17 个可点项 → **一句填空**：

```
我是 [职场进阶 ▾] 的人，
常待在 [编程 ▾] 领域，
最愿意讲 [踩过的坑 ▾]。
```

三个 inline 下拉嵌在一句话里，点完即走，无独立"提交"按钮（选满三项后底部才浮现「好了 →」）。
- 视觉负担从 17 个色块降到 3 个下划线。
- 语气从"填表"变成"自我介绍"。
- 领域改为**单选**（Demo 只需一个），`share_preferences` 也单选。**数据契约不变**，前端提交时包成单元素数组即可。

**（b）一次只给一个问题**

```
                    你的经历里，可能藏着这个问题的答案

        ┌──────────────────────────────────────────────┐
        │                                              │
        │        工作两三年后，                          │
        │        你悟出了哪些职场道理？                   │
        │                                              │
        │  ───────────────────────────────             │
        │                                              │
        │  这个问题最好的答案，往往来自真实的代价。        │
        │  而你说过，你愿意讲踩过的坑。                   │
        │                                              │
        │                                              │
        │              这个，我有话说  →                 │
        │                                              │
        └──────────────────────────────────────────────┘

                 换一个问题                      1 / 3
```

**关键点**：
- 问题标题字号 **34–40px**，是这一屏唯一的主角。
- `why_fits` 改写为**第二人称直接对话**，且必须**回指用户刚才的自我介绍**（"你说过，你愿意讲踩过的坑"）。这是"Why you?"落地的唯一方式——不是形容问题，而是**引用用户自己**。
- CTA **不是按钮块**，是一行带箭头的文字，字号 18px，暖金色，hover 时箭头右移 4px。它要看起来像**一个决定**，不是一个提交动作。
- 「换一个问题」在最底部，14px 灰字。`1 / 3` 给出边界感（不是无限流）。
- 整块卡片可点击 = CTA，扩大热区。
- 演示数据标注移到页脚一行 11px 灰字。

### 后端契约影响

`why_fits` 需要按 profile 动态拼接。Phase 1 可用**模板 + 槽位**实现（不需要 LLM）：

```
DEMO_QUESTIONS[i].why_fits_template =
  "这个问题最好的答案，往往来自真实的代价。而你说过，你愿意讲{share_label}。"
```

这是一处**后端小改动**，但对体验的杠杆很大。

---

## 5. Mining 新设计 — Thought Fragments

### 原则

> **不做 Dashboard。做证物墙。**

用户说的每一句重要的话，被**摘出来、命名、钉在墙上**。少而重，不是多而全。

### Fragment 形态

```
    ┌─────────────────────┐
    │ PROMISE             │   ← 11px，字距 0.14em，暖灰
    │                     │
    │ 年底晋升             │   ← 17px，白，最多 8 字
    └─────────────────────┘
```

- **只有两行**：类型标签 + 极短短语。没有描述、没有时间戳、没有图标、没有计数。
- 短语必须是**用户原话的压缩**，不是 AI 的改写。"年底晋升"来自"领导答应年底升职"。
- 宽度自适应内容，不是等宽卡片列表。让它们看起来像**便签**，不像**表格行**。

### 类型体系（对用户可见的只有 5 种）

| 用户可见 | 映射自 KnowledgeState | 视觉 |
|---|---|---|
| `PROMISE` | beliefs（他人承诺类） | 中性灰 |
| `CHOICE` | facts（用户的决策） | 中性灰 |
| `OUTCOME` | events.result | 中性灰 |
| `COST` | facts（放弃的东西） | 中性灰 |
| `REFLECTION` | reflections | 略亮 |

**`conflicts` 和 `candidate_insights` 不再作为 Fragment 显示**——它们是 Insight 的原料，要留到揭示时刻才出场。这是**惊喜守恒**：提前显示"冲突"就是剧透。

> ⚠️ 这需要后端在 `KnowledgeState` 之外**增加一个面向展示的 `fragments: Fragment[]` 字段**（`{kind, text}`）。
> 内部 `KnowledgeState` 保持不变（LLM 阶段仍需要它），只是**新增一层展示投影**。这是本轮唯一必要的模型扩展。

### Demo 的 Fragment 序列

| 用户第 N 句 | 新增 Fragment |
|---|---|
| 1「别太相信领导画饼」 | *（无。第一句是态度不是素材，让墙保持空白，制造"还没开始"的感觉）* |
| 2「领导答应年底升职，我拒绝了涨薪30%的Offer」 | `PROMISE 年底晋升` + `CHOICE 拒绝 +30% Offer` |
| 3「领导之前对我不错，说得很具体」 | `COST 一次确定的加薪` |
| 4「年底没升职，Offer 也没了」 | `OUTCOME 承诺落空` |
| 5「我把没保证的机会当成确定的了」 | `REFLECTION 把可能当成了确定` |

**共 5 个 Fragment**。这是上限。超过 7 个，每一个就都不重要了。

### 空间位置

**不再是右侧 340px 固定栏。**

访谈时，Fragment 悬浮在**聊天区右侧的留白中**，无边框、无背景板、无标题：

```
┌──────────────────────────────────────────────────────────┐
│  工作两三年后，你悟出了哪些职场道理？                        │
│                                                          │
│  ┌────────────────────────────┐                          │
│  │                            │      ┌──────────────┐    │
│  │  ◈ 发生过什么事，让你开始    │      │ PROMISE      │    │
│  │    有这种感觉？             │      │ 年底晋升      │    │
│  │                            │      └──────────────┘    │
│  │       领导答应年底升职，     │                          │
│  │       所以我拒绝了涨薪30%   │   ┌──────────────────┐   │
│  │       的 Offer。      你 │   │ CHOICE           │   │
│  │                            │   │ 拒绝 +30% Offer  │   │
│  │  ◈ 当时你为什么愿意相信？    │   └──────────────────┘   │
│  │                            │                          │
│  └────────────────────────────┘        ┌───────────┐     │
│                                        │ COST      │     │
│  [ 说说你的真实经历…            ] 发送   │ 一次确定的 │     │
│                                        │ 加薪      │     │
└──────────────────────────────────────────────────────────┘
```

**关键点**：
- Fragment **位置轻微错落**（`translateX` 各差 8–20px），不对齐成列表。错落 = 尚未整理的素材；对齐 = 已归档的记录。**这个差别是整个 Mining 视觉的核心**。
- 没有"Knowledge Panel"标题，没有边框容器。它们**漂在页面上**。
- 聊天气泡去掉圆形头像和聊天泡背景：AI 的话用 `◈` 前缀 + 灰字左对齐，用户的话用白字右对齐 + 极淡底色。**去 chat 化**，让它更像访谈速记。

---

## 6. Insight Reveal 新设计 — 三幕形变

> 这是全站最重要的 20 秒。它必须发生在**同一个空间里**，绝不能是页面切换。

### 触发

用户第 5 句发出、后端返回 `state === 'INSIGHT'` 时，**不改变路由、不切换 section、不弹 Modal**。
在 `MinePage` 内引入一个覆盖态 `revealPhase: 0 | 1 | 2 | 3`，逐幕推进。

### 幕次

**第 0 幕 · 静默（0 → 400ms）**

- 输入框 `opacity: 1 → 0`，向下 8px 淡出。
- 聊天区 `opacity: 1 → 0.12`，`filter: blur(3px)`，**不 unmount**，仍在原位。
- 背景 `--bg` 由 `#07080c` 压到 `#040509`。
- Fragment **保持完全清晰**。

> 这一幕的全部意义：**除了你说过的话，其他都退场了。**

**第 1 幕 · 聚拢（400 → 1600ms）**

- 5 个 Fragment 从各自的错落位置，**移动到画面中央**，重排成一条有意义的**因果链**（不是网格、不是列表）：

```
                PROMISE            CHOICE             OUTCOME
                年底晋升    ──→   拒绝 +30% Offer  ──→  承诺落空
                                     │
                                     ↓
                                   COST
                                一次确定的加薪
```

- 连接线**逐条画出**（`stroke-dashoffset` 动画），每条 240ms，依次进行。
- `REFLECTION 把可能当成了确定` 暂时**留在下方，不接入链条**。
- 用 CSS `transform` + FLIP 实现位移。**Fragment DOM 节点不重建**——同一个元素移动过去，用户才会认出"这就是刚才那张"。

> 这一幕的全部意义：**这些不是散的，它们之间有关系。**

**第 2 幕 · 停顿（1600 → 2600ms）**

- 链条完成后，**画面静止 600ms**。什么都不发生。
- 然后 `COST` 与 `OUTCOME` 两个 Fragment 同时描一层暖金边框，轻微放大到 1.04。
- 中央下方浮出一行小字，**不是 AI 语气，是低语**：

```
                        等一下。这里有点东西。
```

（英文版：`Wait. There's something here.`）
- 字号 15px，`--text-muted`，`opacity 0 → 1` 用 700ms，**慢**。

> 这一幕的全部意义：**制造发现前的那口气。**
> 停顿是这个设计里最贵的部分，不要为了"流畅"删掉它。

**第 3 幕 · 命名（2600 → 4000ms）**

- 链条整体**上移并缩小到 0.82**，退到画面上三分之一，`opacity 0.5`。它变成**证据**。
- 中央出现一句短的**命名**，暖金色，26px：

```
                   你用一次确定的机会，
                   换了一个还没发生的承诺。
```

- 停 800ms 后，下方浮出真正的 Insight，白色，20px，左侧一道 2px 暖金竖线：

```
     │  不要让未经验证的未来承诺，
     │  拥有和现实机会相同的决策权重。
```

- 再停 600ms，底部出现动作：

```
              就是这个            我想改一下
```

（删掉「接近，但还不对」占位按钮）

### 为什么这样能成立

| 设计动作 | 心理效果 |
|---|---|
| 聊天区不 unmount 只 blur | Insight 发生在"我的对话"里面，不是另一个页面 |
| Fragment 是同一批 DOM 移动过去 | 用户认得出"这是我刚才说的" |
| 连接线逐条画 | 关系是**被建立**的，不是**被展示**的 |
| 第 2 幕的 600ms 静止 | 没有停顿就没有 Wow，只有信息 |
| 先给"命名"，后给"Insight" | 命名是**对经历的描述**（我的），Insight 是**从中提炼的原则**（也是我的）。顺序反了就变成 AI 下结论 |
| 链条最后缩小成证据 | 视觉上明说：Insight 站在这些事实之上 |

### 明确否决的做法

- ❌ Modal / Dialog / 遮罩弹窗
- ❌ 撒花、粒子、光晕爆开
- ❌ 打字机逐字输出 Insight（廉价，且拖慢关键信息）
- ❌ `✨` 之类的 emoji 提示
- ❌ 任何以 "AI"、"我" 为主语的提示文案

---

## 7. Community Challenge 新设计 — 对峙

### 布局：左右对称，中间断层

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   YOUR TAKE                    │                    ANOTHER TAKE│
│                                │                                │
│   不要让未经验证的未来承诺，      │      职业早期真正值得押注的机会，  │
│   拥有和现实机会相同的           │      本来就很少是完全确定的。      │
│   决策权重。                    │                                │
│                                │                                │
│                              V S                                │
│                                │                                │
│                                │      来自知乎社区观点（演示数据）  │
│                                │                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│              他哪里说对了？又哪里说错了？                          │
│                                                                 │
│   [                                                        ]    │
│                                                       回应 →     │
└─────────────────────────────────────────────────────────────────┘
```

**关键点**：
- **用户的 V1 必须在场，且在左侧**（阅读起点 = 立足点）。这是修复 Challenge 体验的**第一要务**。
- 中间是一道 1px 竖线 + 中点一个 `V S`（字距 0.3em，14px，`--text-dim`）。不用图标，不用"对比"标签。
- 左右两侧**字号、行高、字重完全相同**。任何一侧更大 = 系统在暗示谁更对。
- **删掉 `challenge_question`**。追问固定为一句：**「他哪里说对了？又哪里说错了？」**
  - 这句话本身就把用户放在**裁判**位置，而不是**被考**位置。
  - 它同时要求用户做两件事（承认 + 反驳），天然导向"限定条件"而不是"站队"，这正是通往 V2 的认知路径。
- `reason` 字段**折叠**到 ANOTHER TAKE 下方，14px 灰字，默认显示但视觉降级——`claim` 才是要对峙的东西。
- **Mock 标注保留**：右下角一行 11px `来自知乎社区观点（演示数据）`。**这是 AGENTS.md P5 的合规红线，不因视觉简化而删除。**

### 入场动效

- YOUR TAKE 从左侧 `translateX(-24px)` 淡入（0–320ms）。
- 中线从中点向上下**展开**（320–560ms）。
- ANOTHER TAKE 从右侧 `translateX(24px)` 淡入（400–720ms）。
- `V S` 最后出现，`scale 0.9 → 1`（720–860ms）。

> 三段错开进入，让"我的观点先站住，然后来了一个对手"。同时进入就没有对峙叙事。

---

## 8. Insight Evolution 新设计 — 原地改写

> 用户必须**看见自己的句子发生了什么**，而不是读两段文字然后自己比对。

### 舞台：只有一段文字，它自己变

删掉 `evo-chain` 的四个节点。整个阶段只有**一句话**在画面中央，它经历三次变化。

**第 1 步 · 复原（0 → 400ms）**

屏幕中央只剩用户的 V1，字号 24px：

```
              不要让未经验证的未来承诺，
              拥有和现实机会相同的决策权重。
```

**第 2 步 · 松动（400 → 1400ms）**

绝对化的词被标出并淡出，**原地留下空缺**：

```
              ~~不要让~~未经验证的未来承诺，
              拥有和现实机会相同的决策权重。
```

- `不要让` 加删除线，`opacity 1 → 0.25`，同时字符宽度收拢。
- 右侧浮出一行 13px 灰字（用户自己回应的压缩）：

```
                          你说：它可以值得押注，
                          但要有兑现条件。
```

**第 3 步 · 落入（1400 → 2800ms）**

新的限定词**从下方逐个升起并嵌入句子**，每个间隔 220ms：

```
              长期机会可以值得冒险，
              但应该根据 [兑现条件] 、 [概率]
              和 [机会成本] ，
              对未来承诺进行风险定价。
```

- `兑现条件` / `概率` / `机会成本` 三个词，各自 `translateY(10px) → 0` + `opacity 0 → 1`，**依次**落位。
- 落位瞬间该词短暂描一层暖金（180ms 后褪回白色）。

**第 4 步 · 定名（2800ms →）**

句子整体轻微上移，上方出现：

```
                    YOUR TAKE, REFINED.
```

- 14px，字距 0.16em，暖金色。
- 下方出现单一动作：`把它写成回答 →`

### 为什么这样能成立

- **同一段文字变形** ⇒ 用户看到的是"我的观点被改进了"，而不是"AI 又生成了一段"。
- **先删后加** ⇒ 精确演示"绝对表达被弱化，限定条件被加入"这个认知动作本身。
- **三个限定词逐个落下** ⇒ 用户能数出来"我多想到了三件事"。可数 = 可感知的成长。
- **`YOUR TAKE, REFINED.` 是标签不是评价** ⇒ 不替用户说"你变锋利了"，让他自己得出。

### 后端契约影响

需要 `Insight` 增加两个可选展示字段（不影响现有逻辑）：

```python
class Insight(BaseModel):
    ...
    softened_span: str | None = None      # V2 用：V1 中被弱化的片段，如 "不要让"
    added_qualifiers: list[str] = []      # V2 用：新增的限定词
```

Phase 1 在 `DEMO_INSIGHT_V2` 里硬编码 `["兑现条件", "概率", "机会成本"]` 即可。

---

## 9. 每个关键阶段的页面线框描述

### W1 · Landing（一屏，无滚动）

```
┌────────────────────────────────────────────────────┐
│                                                    │
│                                                    │
│                       ◈                            │
│                    MindMine                        │
│                                                    │
│           You know more than you think.            │
│                                                    │
│                                                    │
│         我是 职场进阶 的人，常待在 编程 领域，          │
│              最愿意讲 踩过的坑 。                    │
│              ‾‾‾‾‾‾    ‾‾‾‾      ‾‾‾‾‾‾            │
│                                                    │
│                                                    │
│                    好了  →                          │
│                                                    │
│                                                    │
│   Phase 1 演示版本，问题与社区观点为演示数据            │
└────────────────────────────────────────────────────┘
```
元素总数：**6**（现在是 12+）

---

### W2 · Discovery（一屏，无滚动）

```
┌────────────────────────────────────────────────────┐
│                                                    │
│        你的经历里，可能藏着这个问题的答案               │
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │                                              │  │
│  │   工作两三年后，                               │  │
│  │   你悟出了哪些职场道理？                        │  │
│  │                                              │  │
│  │   ────────                                   │  │
│  │                                              │  │
│  │   这个问题最好的答案，往往来自真实的代价。        │  │
│  │   而你说过，你愿意讲踩过的坑。                   │  │
│  │                                              │  │
│  │                          这个，我有话说  →      │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│              换一个问题              1 / 3          │
│                                                    │
│   演示数据                                          │
└────────────────────────────────────────────────────┘
```

---

### W3 · Mining（一屏，聊天内滚动）

```
┌────────────────────────────────────────────────────────────┐
│  ◈  工作两三年后，你悟出了哪些职场道理？        ·  ·  ·      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│   ◈ 发生过什么事，让你开始有这种感觉？                        │
│                                              ┌───────────┐ │
│              领导答应我年底升职，所以我拒绝了     │ PROMISE   │ │
│              另一个涨薪 30% 的 Offer。         │ 年底晋升   │ │
│                                              └───────────┘ │
│   ◈ 当时你为什么愿意相信这个承诺？                            │
│                                          ┌────────────────┐│
│              因为他之前对我不错，说得也很具体。  │ CHOICE         ││
│                                          │ 拒绝 +30% Offer││
│   ◈ 后来呢？                              └────────────────┘│
│                                                            │
│              年底没升职，那个 Offer 也没了。   ┌───────────┐  │
│                                            │ COST      │  │
│                                            │ 一次确定的 │  │
│  ─────────────────────────────────────     │ 加薪      │  │
│  [ 说说你的真实经历…                  ]  →   └───────────┘  │
│    试试：我不是错在相信领导，而是把…                          │
└────────────────────────────────────────────────────────────┘
```
右上角 `· · ·` 是极轻的三点进度（挖掘 / 碰撞 / 成形），无文字标签，hover 才显示。

---

### W4 · Insight Reveal · 第 1 幕（视口锁定）

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│    ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   ← 聊天区 12% 透明 + 模糊 │
│    ╎  ◈ 发生过什么事…             ╎                          │
│    ╎        领导答应我年底升职…     ╎                          │
│    └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘                          │
│                                                            │
│                                                            │
│      ┌─────────┐      ┌────────────────┐   ┌──────────┐    │
│      │ PROMISE │ ───→ │ CHOICE         │──→│ OUTCOME  │    │
│      │ 年底晋升 │      │ 拒绝 +30% Offer│   │ 承诺落空  │    │
│      └─────────┘      └────────┬───────┘   └──────────┘    │
│                                │                           │
│                                ↓                           │
│                          ┌───────────┐                     │
│                          │ COST      │                     │
│                          │ 一次确定的加薪│                    │
│                          └───────────┘                     │
│                                                            │
│                  ┌──────────────────┐                      │
│                  │ REFLECTION       │  ← 暂不接入链条        │
│                  │ 把可能当成了确定   │                      │
│                  └──────────────────┘                      │
└────────────────────────────────────────────────────────────┘
```

### W5 · Insight Reveal · 第 3 幕

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│     ┌───┐→┌───┐→┌───┐                                      │
│     └───┘ └─┬─┘ └───┘      ← 链条缩至 0.82、opacity 0.5     │
│           ┌─┴─┐                                            │
│           └───┘                                            │
│                                                            │
│                                                            │
│                你用一次确定的机会，                           │
│                换了一个还没发生的承诺。        ← 26px 暖金     │
│                                                            │
│                                                            │
│         │  不要让未经验证的未来承诺，                          │
│         │  拥有和现实机会相同的决策权重。   ← 20px 白 + 金竖线  │
│                                                            │
│                                                            │
│                就是这个          我想改一下                   │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### W6 · Challenge

见 §7 线框。

### W7 · Evolution · 第 3 步

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│                                                            │
│                                                            │
│           长期机会可以值得冒险，                              │
│           但应该根据 兑现条件 、 概率                          │
│                     ‾‾‾‾‾‾   ‾‾‾  ← 依次升起，落位闪金       │
│           和 机会成本 ，                                     │
│              ‾‾‾‾‾‾                                        │
│           对未来承诺进行风险定价。                             │
│                                                            │
│                              你说：它可以值得押注，            │
│                              但要有兑现条件。   ← 13px 灰     │
│                                                            │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

### W8 · Result

```
┌────────────────────────────────────────────────────────────┐
│                                                            │
│         │  长期机会可以值得冒险，但应该根据兑现条件、           │
│         │  概率和机会成本，对未来承诺进行风险定价。            │
│                                                            │
│         从「别太相信领导画饼」开始                             │
│                                                            │
│  ──────────────────────────────────────────────            │
│                                                            │
│  回答 · 工作两三年后，你悟出了哪些职场道理？        655 字      │
│                                                            │
│  我曾经因为一个口头承诺，拒绝了一个涨薪 30% 的 Offer。         │
│  ……                                                        │
│                                                            │
│  ──────────────────────────────────────────────            │
│                                                            │
│   复制回答      编辑        去知乎发布                        │
│                                                            │
│   真实知乎问题链接将在 Zhihu API 接入阶段启用                   │
└────────────────────────────────────────────────────────────┘
```
- 删掉 `STAGE 6 — 完成`、"这是你自己想明白的答案"、"没有一句是凭空生成的"。
- Insight 在顶部，用与 Evolution 完全相同的金竖线样式 —— **视觉延续性证明它就是刚才那句**。
- "从「…」开始" 一行，闭合首尾。

---

## 10. 动效应该发生在哪里

**只在三个地方投入动效预算。** 其余全部用 120–180ms 的朴素过渡。

| 优先级 | 位置 | 动效 | 时长 | 目的 |
|---|---|---|---|---|
| **P0** | Insight Reveal 聚拢 | Fragment FLIP 位移 + 连接线 `stroke-dashoffset` 逐条绘制 | 1200ms | 证明关系是被建立的 |
| **P0** | Insight Reveal 停顿 | **静止**（这也是动效设计） | 600ms | 制造发现前的呼吸 |
| **P0** | Insight Reveal 环境 | 聊天区 `opacity/blur`、背景压暗 | 400ms | 空间转变而非页面切换 |
| **P1** | Evolution 改写 | 删除线 + 字符收拢 + 限定词逐个 `translateY` 落入 | 2400ms | 让"想深一层"可见 |
| **P1** | Challenge 入场 | 左 → 中线 → 右 → `V S` 四段错开 | 860ms | 建立对峙叙事 |
| **P2** | Fragment 出现 | `scale 0.94 → 1` + `opacity`，带 40ms 随机延迟 | 260ms | 像被"钉上去"，不是被渲染 |
| **P2** | CTA hover | 箭头 `translateX(4px)` | 160ms | 指向性 |
| **P2** | Discovery 换题 | 旧题淡出 + 新题淡入（不滑动） | 240ms | 干净 |

**全局约束**：
- 缓动统一 `cubic-bezier(0.22, 0.61, 0.36, 1)`（ease-out 系），**禁止 bounce / elastic**。这是一个严肃产品。
- 必须实现 `@media (prefers-reduced-motion: reduce)`：所有位移动画降级为直接淡入，**但 Insight 三幕的顺序和停顿保留**（用 `setTimeout` 控制，不依赖 transition）。叙事节奏不是装饰，不能被 reduce-motion 抹掉。

---

## 11. 哪些动效没有价值，不要做

| ❌ 不做 | 原因 |
|---|---|
| Insight 文字打字机逐字输出 | 廉价的"AI 感"，且延迟关键信息，与"这是我的话"相悖 |
| 粒子 / 光斑 / 星屑 / 撒花 | 庆祝式动效会把认知发现降格成游戏奖励 |
| 数字滚动（字数、Fragment 计数） | 计数本身就要删掉 |
| Fragment 常驻浮动 / 呼吸 / 视差 | 持续运动分散注意，且暗示"这些还在变"，与"这是已确定的事实"矛盾 |
| 页面切换的 slide / 3D flip | 强化"换页"心理模型，与"同一空间形变"直接冲突 |
| 骨架屏 shimmer | Mock 响应 < 50ms，闪一下比不闪更糟 |
| 按钮 ripple / 光泽扫过 | 装饰性，无信息 |
| 背景渐变缓慢流动 | 分散注意，且已有的 `body::before` 双 radial 已经够了 |
| Insight 卡片发光脉冲 | 用亮度而非结构制造重要性，是最偷懒的做法 |
| 滚动触发的入场动画 | 关键阶段应锁定视口，不该有滚动 |

---

## 12. 需要修改哪些现有组件

### 前端

| 文件 | 动作 | 说明 |
|---|---|---|
| `components/KnowledgePanel.vue` | **删除** | 被 Fragment 系统完全取代 |
| `components/ThoughtFragment.vue` | **新建** | 单个 Fragment，props: `kind` / `text` / `state('idle'\|'linked'\|'highlight'\|'evidence')` |
| `components/FragmentField.vue` | **新建** | Fragment 容器。负责错落布局 → 因果链的 FLIP 重排，以及 SVG 连接线绘制。**Insight Reveal 的技术核心** |
| `components/InsightReveal.vue` | **新建** | 三幕时序控制器。接收 fragments + insight，驱动 `revealPhase` |
| `components/TakeCollision.vue` | **新建** | YOUR TAKE / VS / ANOTHER TAKE 对峙布局 |
| `components/InsightRewrite.vue` | **新建** | V1 → V2 原地改写动画 |
| `pages/MinePage.vue` | **重写**（857 → 约 320 行） | 移除 4 个 `v-if` 分支的巨型内联结构；改为单一舞台 + 阶段编排；删除 `STATE_STEPS`；聊天区去 chat 化 |
| `pages/QuestionsPage.vue` | **重写**（231 → 约 140 行） | 三卡 → 单问题 + 换一个 |
| `pages/LandingPage.vue` | **重写**（268 → 约 160 行） | 17 选项 → 三处 inline 下拉填空 |
| `pages/ResultPage.vue` | **中改** | 删阶段标签与解释性副标题；Insight 上提并复用金竖线样式 |
| `styles/global.css` | **中改** | 新增 Fragment / 对峙 / Reveal 三组变量与缓动；删除 `.q-card` 等废弃类；补 `prefers-reduced-motion` |
| `stores/session.ts` | **小改** | 新增 `fragments` 状态与 `revealPhase` 编排状态 |
| `types/index.ts` | **小改** | 新增 `Fragment` / `FragmentKind`；`Insight` 加 `softened_span` / `added_qualifiers` |
| `api/client.ts` | **不改** | 端点不变 |
| `router/index.ts` | **不改** | 路由不变 |

### 后端（最小改动，不触碰状态机）

| 文件 | 动作 | 说明 |
|---|---|---|
| `models/session.py` | **小改** | 新增 `Fragment(kind, text)`；`Session` 与 `PostMessageResponse` 增加 `fragments: list[Fragment]`；`Insight` 增加 `softened_span` / `added_qualifiers` |
| `services/demo_content.py` | **中改** | `INTERVIEW_SCRIPT` 每轮补 `fragments` 增量；`DEMO_QUESTIONS` 的 `why_fits` 改为带 profile 槽位的模板；`DEMO_INSIGHT_V2` 补两个新字段 |
| `services/session_store.py` | **小改** | 累积 fragments；`listQuestions` 时用 profile 渲染 `why_fits` |
| `routers/sessions.py` | **不改** | 端点与签名不变 |
| `main.py` | **不改** | — |

**状态机 `PROFILE → … → DONE` 完全不变。** 本轮是展示层重构 + 一层展示投影，不动业务逻辑。

### 工作量估计

| 块 | 预估 |
|---|---|
| 后端展示投影（Fragment / why_fits 模板 / V2 字段） | 1.5h |
| `FragmentField` FLIP + 连接线（技术难点） | 3h |
| `InsightReveal` 三幕时序 | 2.5h |
| `TakeCollision` | 1h |
| `InsightRewrite` | 2h |
| `MinePage` 重写与编排 | 2.5h |
| Landing / Questions / Result 重写 | 2.5h |
| 样式统一 + reduced-motion + 联调 | 2h |
| **合计** | **约 17h** |

---

## 13. 验收标准（体验层）

重构完成后，以下每一条都应成立：

1. Discovery 屏上**同时只有一个问题**，且 `why_fits` 里出现了用户刚才自我介绍中的词。
2. 访谈全程屏幕上**看不到** `facts` / `beliefs` / `conflicts` / `candidate_insights` 任何一个字段名。
3. Insight 发生时**没有新页面、没有 Modal**；聊天记录仍在原位（模糊态）。
4. Insight 聚拢时移动的 Fragment 与访谈时出现的**是同一批 DOM 节点**（可用 DevTools 验证节点未重建）。
5. 从"聚拢完成"到"命名出现"之间，存在一段 **≥600ms 的完全静止**。
6. Challenge 屏上，用户的 V1 与社区观点**左右并置、字号一致**，中间有 `V S`。
7. Challenge 的追问是**「他哪里说对了？又哪里说错了？」**，而不是 AI 转述的问题。
8. V1 → V2 是**同一段文字发生形变**，用户能明确指出被删掉的词和新增的三个限定词。
9. 全站**看不到** `STAGE 2` / `STAGE 6` 之类的内部阶段编号，也看不到 6 段式状态机进度条。
10. `is_mock` 合规标注在 Discovery 页脚与 Challenge 对峙卡下方**依然存在**。
11. 「去知乎发布」仍然只提示，不伪造发布。
12. `prefers-reduced-motion: reduce` 下，Insight 三幕的**顺序与停顿仍在**，只是位移变淡入。

---

## 14. 已识别风险

| 风险 | 影响 | 缓解 |
|---|---|---|
| FLIP 重排在 Fragment 数量变化时容易跳变 | Insight 核心动效失真 | 固定 Demo 为 5 个 Fragment；用 `getBoundingClientRect` 快照 + `transform` 反向补偿；先做静态布局再加动画 |
| Insight 三幕总长 4s，Demo 现场可能显得慢 | 评委失去耐心 | 加**点击跳过**：任意点击立即推进到下一幕（不是直接跳到终态） |
| SVG 连接线在响应式宽度下错位 | 视觉断裂 | 连接线用绝对定位 SVG 覆盖层 + `ResizeObserver` 重算；< 900px 宽度时链条改为垂直排列 |
| 删掉 KnowledgePanel 后，"系统在工作"的感知变弱 | 用户以为没反应 | Fragment 出现动效本身就是反馈；且第 2 句起就有两个 Fragment 同时钉上 |
| `why_fits` 模板化后可能读起来生硬 | Discovery 说服力下降 | 每个 share_preference 写一版专属句式，不做通用槽位 |
| 后端新增字段与现有前端类型不同步 | 类型检查失败 | 遵循 `AGENTS.md` 约定：Pydantic 与 TS 类型同一次提交内同步修改 |

---

## 15. 建议的实施顺序

若确认此方案，建议按**风险从高到低**推进，先攻技术难点：

1. **后端展示投影**（Fragment / V2 字段 / why_fits 模板）—— 打通数据
2. **`FragmentField` 静态布局**（错落 + 链条两种态，无动画）—— 验证视觉
3. **`FragmentField` FLIP + 连接线动画** —— 技术难点，早失败早调整
4. **`InsightReveal` 三幕时序** —— 第一个 Wow
5. **`TakeCollision`** —— 结构简单
6. **`InsightRewrite`** —— 第二个 Wow
7. **`MinePage` 编排整合**
8. **Landing / Questions / Result**
9. **样式统一 + reduced-motion + 端到端验证**

---

## 请确认

在开始实现之前，需要你确认以下几点：

1. **是否同意删除 `KnowledgePanel`**（§2 D1/D2）？这是最大的一处删除。
2. **Insight 三幕总时长约 4 秒**是否可接受？（可点击跳过）
3. **Landing 画像从 17 个选项压到 3 个单选下拉**，是否会影响你后续接 LLM 时的 profile 精度需求？
4. **后端新增 `fragments` 展示投影字段**是否同意？（不改状态机，只加一层）
5. 是否有你特别在意、但我在这份提案里**删掉了**的东西？

确认后我再开始改代码。
