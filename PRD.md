# MindMine 产品需求文档（PRD）

> **版本**：0.1.0-draft
> **日期**：2026-09-13
> **状态**：待确认，确认后方可进入编码

---

## 1. 产品愿景

**MindMine 是一个知识发现工具，不是 AI 写作助手。**

它的立足点是一个观察：大多数人系统性地低估了自己知道的东西。多年积累的真实经历——踩过的坑、学到的教训、察觉的规律——之所以一直没被说出来，是因为没有人按正确的顺序问过正确的问题。MindMine 负责问这些问题。

它的产出不是一篇 AI 生成的答案，而是**用户自己的答案**——通过结构化对话被梳理清楚，再经由真实的社区分歧被打磨锋利。

**Slogan**：*You know more than you think.*（你知道的比你以为的多）

---

## 2. 用户问题

### 「我有话想说，但我没说」

- 在知乎刷到一个自己真能回答的问题，却没动手——因为「应该有人说得比我好吧」。
- 有真实经历，但没有把它变成一个连贯观点的方法。
- 写了草稿，回头一读，删掉了——因为「听起来太普通了」。

### 现有工具为什么解决不了

现有的 AI 写作助手让这件事变得更糟，而不是更好。它们生成的文字很流畅、听起来像用户，却不包含用户任何真实的经历。结果是一篇技术上正确、情感上空洞、而且最关键的——**不是用户真正想法**的答案。

MindMine 的约束同时也是它的产品主张：**系统不得虚构用户经历。** 最终答案里的每一句话，都必须能追溯到用户说过的某句话。

---

## 3. 核心价值

| 面向谁 | 提供什么价值 |
|---|---|
| 有领域经验但表达不出来的从业者 | 把你实际知道的东西挖出来、说清楚 |
| 被专家声音震慑的新人 | 发现你的特定角度恰恰因为「特定」才有价值 |
| 试过写知乎回答但半途放弃的人 | 打通「我有话说」到「我说出来了」的最后一公里 |

---

## 4. 用户画像

### 主要用户：有经验但说不清的人

- 在某个领域（工程、产品、金融、教育等）有 2–8 年从业经验
- 知乎的活跃读者，沉默的贡献者
- 口头聊天时观点很强，落到文字上就卡壳
- 被那些几百赞的精致长回答劝退
- 典型心声：「我说不太清楚，但我知道大家说的那套不太对」

### 次要用户：爱反思的新人

- 0–2 年经验
- 拥有一个资深贡献者早已忘记自己曾经有过的新鲜视角
- 低估了「我刚经历完这件事，这是真正有用的部分」的价值
- 最可能从「你最近的亲身经历才是差异点」这个认知中获益

### MVP 不覆盖

- 以涨粉为目标的专业内容创作者
- 想让 AI 代笔写答案的用户
- 与知乎没有任何связь的用户

---

## 5. 完整用户旅程

### 总览

```
Stage 1：Know Me         — 用 OAuth 数据或手动输入建立轻画像
Stage 2：Find My Q       — 推荐 3 个用户可能真的有话说的问题
Stage 3：Mine My Exp     — 结构化访谈，挖出真实经历
Stage 4：Insight Moment  — 「你真正想说的是这个」（Wow Moment 1）
Stage 5：Community Chal  — 真实知乎观点挑战用户的洞察
Stage 6：Refine & Comp   — 用户回应，洞察升级，生成回答
                           （Wow Moment 2 + 发布流程）
```

### Stage 1 — Know Me（认识你）

**目标**：不让用户填表，也能知道他是谁。

**主路径（OAuth）**：

1. 用户发起知乎 OAuth 登录。
2. 后端拉取：基本资料（`/user`）、关注列表、近期收藏、个人创作。
3. LLM（`ProfileExtractor`）综合出一个轻量画像：

```json
{
  "domains": ["AI 工程", "前端开发"],
  "experience_level": "mid_career",
  "interests": ["AI 工具", "职业转型"],
  "share_preferences": ["踩过的坑", "对比分析"]
}
```

4. 界面呈现：「我们发现你可能很懂这些东西」+ 3–4 张领域卡片。
5. 用户可以**修改、删除、新增**卡片，然后继续。

**兜底路径（无 OAuth）**：

三个问题的手动表单：

- 「你从事或最关心哪个领域？」
- 「你的经验大概是什么阶段？」（刚入行 / 有几年 / 资深 / 纯好奇）
- 「你更愿意分享哪类内容？」（踩过的坑 / 对比分析 / 方法论 / 观点）

**输出**：一个 `UserProfile` 对象，传给 Stage 2。

---

### Stage 2 — Find My Question（找到我的问题）

**目标**：给出 3 个用户真的会想回答的问题。

**流程**：

1. `QuestionMatcher` 根据画像生成 2–4 个 query 词（如「AI 编程工具」「前端工程师职业成长」）。
2. 对每个 query 调用 `question recommend --query <q> --count 5`（**真实 API**）。
3. 按 URL 去重，收集约 10–15 个候选。
4. LLM 依据用户画像和 `share_preferences` 重排序。
5. 展示前 3 个作为 Question Card。

**Question Card 界面**：

- 问题标题（来自知乎 API）
- 一句话「为什么适合你」（由 `QuestionMatcher` 生成，必须基于用户画像）
- 「我有话说」按钮

**兜底**：API 失败或返回不足 3 条时，从 20 个预置问题（覆盖 5 个领域）中补齐。

**约束**：不得展示用户完全没表达过任何关联的领域问题。

---

### Stage 3 — Mine My Experience（挖掘我的经历）

**目标**：通过结构化对话，挖出用户立场背后的真实经历。

**状态机**：

```
DISCOVERY → EXPERIENCE → CONFLICT → REFLECTION → INSIGHT
```

每个状态对应不同的提问策略：

| 状态 | AI 的目标 | 提问类型举例 |
|---|---|---|
| DISCOVERY | 先搞清用户到底知道什么 | 「你在 X 这件事上的实际经历是什么？」 |
| EXPERIENCE | 获取具体事件和事实 | 「能说一个具体的时刻吗，当时……」 |
| CONFLICT | 找到张力或意外 | 「你原本以为会怎样，实际发生了什么？」 |
| REFLECTION | 理解他的结论 | 「现在回头看，你觉得真正的原因是什么？」 |
| INSIGHT | 把原则结晶出来 | 「如果只能说一件你学到的事……」 |

**每轮规则**：

- AI 每轮**只提一个**问题。
- 提问遵循 WHAT → WHY → CONFLICT → RESULT → REFLECTION → PRINCIPLE 的推进弧线。
- AI **不得断言**用户的经历（「听起来你当时感觉很 X」只能是一个待确认的假设，不能当作事实记录）。
- 用户的原话作为 `USER_QUOTE` 类型证据单独保留。

**KnowledgeState**（贯穿 Stage 3 及之后）：

```json
{
  "session_id": "...",
  "question_url": "https://www.zhihu.com/question/...",
  "question_title": "...",
  "facts": [
    {"text": "...", "source": "USER_QUOTE", "turn": 3}
  ],
  "events": [],
  "beliefs": [],
  "conflicts": [],
  "reflections": [],
  "candidate_insights": []
}
```

**推进条件**：当 KnowledgeState 在 `facts`、`events`、`conflicts`、`reflections` 四项中各至少有一条时，进入 Stage 4。

---

### Stage 4 — Insight Moment（洞察时刻）★ Wow Moment 1

**目标**：给用户「被读懂了」的体验。

**触发**：Stage 3 的 INSIGHT 状态完成。

**界面节奏**：

1. 一行字缓缓淡入：*「我好像发现你真正想说的东西了。」*
2. 揭示：
   - **Surface Claim**（一句话，「暴论」版本）
   - **Deep Insight**（2–3 句，「为什么这对我成立」版本）
3. 用户操作按钮：
   - ✓「就是这个」→ 确认，进入 Stage 5
   - ≈「接近，但还不对」→ 带着具体待细化字段回到 Stage 3
   - ✎「我要修改」→ 自由文本编辑，然后确认

**规则**：Stage 6 使用、以及最终写进答案的 Insight，必须是**用户明确确认过的那个版本**——不是 AI 的版本，是用户确认的版本。

---

### Stage 5 — Community Challenge（社区挑战）

**目标**：找到一个真正能挑战用户已确认洞察的真实知乎观点。

**数据管线**（在 Stage 4 期间/之后后台执行）：

1. `search zhihu --query "<问题标题>" --count 10`（**真实 API**）
2. 按 URL 中的问题 ID 过滤结果
3. 同时调用 `question answers --question-url "<url>" --limit 10`（**真实 API**，补充覆盖面）
4. 合并、按回答 ID 去重、执行作者去重（同一作者最多 2 条）
5. `PerspectiveExtractor`（LLM）提取 `CommunityPerspective` 对象——**只能基于 `ContentText` 中的证据**；防幻觉守卫会拒绝生成文本不支持的主张
6. `ChallengeSelector`（LLM）挑出最能挑战用户洞察的那一条

**CommunityPerspective 数据结构**：

```json
{
  "id": "uuid",
  "claim": "...",
  "reason": "...",
  "author": "...",
  "badge": "...",
  "authority_level": "...",
  "source_url": "...",
  "evidence_text": "..."
}
```

**挑战界面**：

- 标题：*「知乎上有人这么说——」*
- 观点卡片：claim + reason + 作者徽章 + 原文链接
- 提问：*「你怎么看？这改变了你的判断吗？」*
- 用户自由文本回应

**如果不存在真正的对立观点**：改为 *「另一种值得考虑的视角」*——**不要为了反对而制造反方**。

**兜底**：知乎 API 失败时，从 `demo_evidence.json` 按问题领域加载预置观点。

---

### Stage 6 — Refine & Compose（打磨与成文）★ Wow Moment 2

**目标**：让用户看见自己的思考被理清的过程。

**流程**：

1. 展示演进链路：Insight V1 → Community Challenge → 用户回应 → **Insight V2**
2. `InsightRefiner`（LLM）整合用户对挑战的回应，生成 Insight V2。它只能使用 `KnowledgeState` 和用户的挑战回应中的材料，不得引入外部内容。
3. V2 揭示的界面节奏：*「这才是你真正想说的。」*
4. `AnswerComposer`（LLM）生成完整知乎回答，只能使用：
   - `USER_FACT`、`USER_EVENT`、`USER_QUOTE`、`USER_INSIGHT`、`USER_RESPONSE_TO_CHALLENGE`、`COMMUNITY_EVIDENCE`
   - 所有生成的主张必须带来源标签。**无法溯源的句子必须删掉，而不是改写。**
5. 最终页面：
   - Insight V2（高亮）
   - 完整回答正文
   - [编辑回答] — 打开内联编辑器
   - [复制回答] — 复制到剪贴板
   - [去知乎发布] — 新标签页打开 `https://www.zhihu.com/question/{id}`

**产品文案硬规则**：按钮文案是「去知乎发布」，**永远不是「一键发布」**。提示语：「我们会帮你打开知乎页面，你来发布。」

---

## 6. 页面清单

| 页面 | 路由 | 关键元素 |
|---|---|---|
| 落地页 | `/` | 价值主张、「开始」按钮、可选的知乎 OAuth 登录 |
| 认识你 — OAuth | `/onboard/oauth` | 拉取画像时的加载态、可编辑的领域卡片 |
| 认识你 — 手动 | `/onboard/manual` | 3 个问题的表单、进度指示 |
| 找到我的问题 | `/questions` | 3 张 Question Card：标题、为什么适合你、按钮 |
| 访谈 | `/interview/:sessionId` | 对话式轮次、状态指示器、轮次计数 |
| 洞察时刻 | `/insight/:sessionId` | 全幅揭示动画、Surface Claim、Deep Insight、3 个操作按钮 |
| 社区挑战 | `/challenge/:sessionId` | 观点卡片、自由文本回应、跳过选项 |
| 打磨成文 | `/compose/:sessionId` | V1→挑战→回应→V2 时间线、完整回答、3 个操作按钮 |

---

## 7. 两个 Wow Moment 设计说明

### Wow Moment 1：洞察揭示（Stage 4）

用户刚刚用 5–10 轮对话、以碎片的方式描述了自己的经历。他很可能还没有把自己的立场连贯地说出来过。洞察揭示要让他看见自己的立场被清晰陈述——这种感觉应该是**认出**，而不是**被生成**。

**设计要求**：界面必须让人感到「发现」而非「交付」。放慢节奏，留白，**不要用流式打字光标**——短暂停顿后，让完整的想法一次性出现。

**必须避免的失败模式**：一个听起来很厉害、但其实并非基于用户所说内容的洞察。防幻觉守卫正是为此存在。

### Wow Moment 2：V2 揭示（Stage 6）

用户看到自己最初的洞察，然后看到它在遭遇真实挑战后变成了什么。V1 和 V2 之间的差量，就是这次会话真的发生了什么的证据。

**设计要求**：V1 与 V2 并列或依次呈现，让「成长」这件事可见。如果 V1 和 V2 完全一致（用户的洞察经受住了挑战），这同样有效、同样值得展示——*「你的判断经受住了挑战」*。

---

## 8. MVP 范围

### P0 — 必须交付（缺失即无法演示）

- Stage 1：手动画像兜底（OAuth 属于 P1）
- Stage 2：`question recommend --query` 问题推荐 + Mock 兜底
- Stage 3：访谈引擎 + KnowledgeState（最少 4 个状态：DISCOVERY、EXPERIENCE、REFLECTION、INSIGHT）
- Stage 4：洞察时刻揭示（Wow Moment 1）
- Stage 5：社区挑战，至少 1 条真实知乎观点 **或** 预置证据兜底
- Stage 6：回答生成 + 复制 + 打开知乎 URL
- 演示稳定性：所有真实服务失败时，仍能用 Mock 跑完整链路

### P1 — 有时间就做

- Stage 1：知乎 OAuth 登录 + 从关注/收藏/创作提取画像
- Stage 3：完整 5 状态机（含 CONFLICT 状态）
- Stage 5：完整观点管线，含作者去重与多样性打分
- 界面打磨：转场、Wow Moment 动画

### P2 — MVP 之后

- 用 `knowledge upload` 把 Insight V2 存入知乎知识库，作为个人洞察资产
- 多会话历史（用户回到之前的挖掘记录）
- 不带 `--query` 的 `question recommend`（画像模式，已知单账号限制）
- 分享 / 导出功能

### 明确不做

- 真正的一键自动发布到知乎（**无此 API**，且永远不会叫「一键发布」）
- 通过 API 修改或删除回答（不存在）
- 任何不基于用户所提供材料的 AI 生成内容

---

## 9. Demo 叙事

**目标受众**：知乎黑客松评委。3 分钟现场演示。

**主角**：Vicky，4 年前端经验的程序员，对「AI 工具到底让开发者更强了，还是只是更快了」持怀疑态度。

**脚本**：

1. *[Stage 1]* 手动画像：「前端」「有几年经验」，偏好分享「踩过的坑和对比」。展示领域卡片：前端开发、AI 工具、职业成长。

2. *[Stage 2]* 三个问题出现。其中一个是：*「如果人人都可以通过 AI 写代码，程序员还需要存在吗？」* Vicky 点击「我有话说」。

3. *[Stage 3]* 4–5 轮对话。AI 挖出一个具体事件：Vicky 用 AI 写了一个功能，上线了，两周后炸了生产环境——因为她根本不理解那段代码在做什么。关键原话：*「我当时根本不知道它在做什么，就合进去了。」*

4. *[Stage 4]* 洞察揭示：*「AI 让你输出得更快，但没有让你理解得更深。快和深是两件事，而产线要的是深。」* Vicky 点击「就是这个」。

5. *[Stage 5]* 挑战：张文保的观点——*「承担后果的能力是 AI 无法替代的分界线。」* Vicky 回应：*「对，而且这不只是责任感的问题——你得真的看懂，才能承担。」*

6. *[Stage 6]* V2 洞察生成，完整回答成文。Vicky 点击「去知乎发布」——浏览器打开该问题页面。

**演示兜底**：任何真实 API 失败时，演示会话无缝从 `demo_session.json` 加载。评委看到的体验完全一致。

---

## 10. 成功指标

### 参与度（会话质量）

| 指标 | 目标 |
|---|---|
| 走到 Stage 4（洞察时刻）的会话占比 | > 60% |
| Stage 3 到洞察前的平均轮次 | 4–8 轮 |
| 首个洞察即点「就是这个」的用户占比 | > 40% |
| 完成 Stage 6（生成回答）的会话占比 | > 30% |

### 产出质量（内容完整性）

| 指标 | 目标 |
|---|---|
| 含至少一条 USER_QUOTE 的回答占比 | 100% |
| 被防幻觉守卫标记的回答占比 | < 5% |
| 带有效 source_url 的社区观点占比 | 100% |

### 演示可靠性

| 指标 | 目标 |
|---|---|
| 端到端演示完成率（含兜底） | 100% |
| 现场演示中兜底被触发的比例 | 0%（走真实 API） |

### 产品质量红线

- 用户可以查看并编辑每一项被推断出的画像内容。
- 进入社区挑战前，用户必须明确确认 Insight V1。
- 生成 V2 前，用户必须对挑战作出回应或明确跳过。
- 「去知乎发布」永远只是打开问题页面，绝不宣称内容已发布。
- 展示给用户的每一条社区观点，都必须附带可点击的原文链接。

---

## 11. 非目标

MindMine 不是：

- 以「生成流畅文本」为核心价值的 AI 写作助手
- 一个通用聊天机器人
- 知乎自动发布工具
- 用户判断力或作者身份的替代品
- 性格测试或冗长的引导问卷
- 社区内容审核或推荐排序系统

产品必须始终能回答这个问题：**「这份产出里，哪一部分来自我？」**
